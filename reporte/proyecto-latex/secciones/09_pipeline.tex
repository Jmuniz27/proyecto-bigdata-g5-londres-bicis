\section{Diseño del pipeline ETL}
\label{sec:pipeline}

\begin{pendiente}
El enunciado pide únicamente el diagrama, sin implementación. Lo que sigue es una propuesta de partida basada en lo que ya se construyó en la Fase 3, con Databricks sobre Azure como plataforma concreta para cada etapa; el equipo debe validarla y ajustarla.
\end{pendiente}

El ETL medido en la Fase 3 ya es, de hecho, un pipeline de una sola capa. Formalizarlo en capas lo vuelve reutilizable y explica las decisiones de diseño. Se asume un escenario de ingesta periódica: el operador del sistema de bicicletas entrega un archivo mensual con los viajes nuevos, de un tamaño similar a la versión S del dataset (0.488 GB, 3.97 millones de filas). Con esa cadencia, el archivo acumulado en la capa bronce alcanza el tamaño de la versión XL (4.725 GB) alrededor del mes 10, momento que se usa como estado estable para el análisis de costos de la Sección \ref{sec:costos}.

\begin{figure}[htbp]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=1.0cm and 1.0cm,
  box/.style={draw, rounded corners, align=center, minimum height=2.4cm, minimum width=2.5cm, fill=blue!4, font=\footnotesize, text width=2.3cm},
  storebox/.style={box, fill=blue!6},
  computebox/.style={box, fill=orange!8},
  arr/.style={-{Stealth[length=2.2mm]}, thick}
]
  \newcommand{\storebadge}[1]{\textcolor{blue!55!black}{\scriptsize\bfseries #1}}
  \newcommand{\computebadgetxt}[1]{\textcolor{orange!55!black}{\scriptsize\bfseries #1}}

  \node[box] (A) {\\[10pt] CSV mensual\\ \texttt{london.csv}\\ (fuente externa)};

  \node[computebox, right=of A] (B)
    {\computebadgetxt{Databricks Workflows}\\[2pt]
     Ingesta\\ trigger mensual\\ programado};

  \node[storebox, right=of B] (C)
    {\storebadge{ADLS Gen2}\\[2pt]
     Bronce\\ \texttt{bronze/}\\ CSV, 9 columnas};

  \node[computebox, right=of C] (D)
    {\computebadgetxt{Databricks Jobs}\\[2pt]
     Limpieza\\ notebook PySpark\\ cast, nulos};

  \node[storebox, right=of D] (E)
    {\storebadge{ADLS Gen2}\\[2pt]
     Plata\\ \texttt{silver/}\\ Delta Lake};

  \node[computebox, right=of E] (F)
    {\computebadgetxt{Databricks Jobs}\\[2pt]
     Agregación\\ mismo notebook\\ \texttt{GROUP BY}};

  \node[storebox, right=of F] (G)
    {\storebadge{ADLS Gen2}\\[2pt]
     Oro\\ \texttt{gold/}\\ Q1 Q2 Q3};

  \node[storebox, above=of D, fill=green!5] (H)
    {\storebadge{ADLS Gen2}\\[2pt]
     Catálogo\\ estaciones\\ 802 filas};

  \node[computebox, above=of E, fill=violet!6] (J)
    {\computebadgetxt{Unity Catalog}\\[2pt]
     Esquema\\ de plata y oro};

  \node[box, right=of G, fill=teal!6] (I)
    {\storebadge{Databricks SQL}\\[2pt]
     Consumo\\ SQL Serverless +\\ Power BI};

  \draw[arr] (A) -- (B);
  \draw[arr] (B) -- (C);
  \draw[arr] (C) -- (D);
  \draw[arr] (D) -- (E);
  \draw[arr] (E) -- (F);
  \draw[arr] (F) -- (G);
  \draw[arr] (H) -- (D);
  \draw[arr] (E) -- (J);
  \draw[arr] (G) -- (I);
\end{tikzpicture}%
}
\caption{Pipeline ETL propuesto sobre Databricks + Azure, capas medallón (bronce/plata/oro) en Delta Lake sobre Azure Data Lake Storage Gen2, con Databricks Jobs como motor de transformación, Unity Catalog como catálogo de esquema y Databricks SQL Serverless para el consumo.}
\label{fig:pipeline}
\end{figure}

\subsection{Herramientas y servicios por etapa}

\begin{table}[htbp]
\centering
\footnotesize
\caption{Servicios de Databricks sobre Azure asignados a cada etapa del pipeline}
\label{tab:pipeline-herramientas}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{2.1cm}>{\raggedright\arraybackslash}p{3.4cm}X}
\toprule
Etapa & Servicio & Por qué esa herramienta \\
\midrule
Orquestación & Databricks Workflows (Jobs) & Programa el job mensual sobre el mismo clúster que ejecuta el ETL; sin servicio de orquestación aparte encendido de forma permanente. \\
Ingesta / Bronce & ADLS Gen2 (contenedor \texttt{bronze/}) & Almacenamiento jerárquico, barato y duradero para el CSV tal cual llega. También aloja el catálogo de estaciones. \\
Limpieza y transformación & Databricks Jobs (notebook PySpark) & Motor Spark administrado: resuelve casteo, nulos y proyección a 4 columnas; el mismo trabajo de Fase 3, escalado a un job programado. \\
Plata & ADLS Gen2 (\texttt{silver/}), Delta Lake & Sigue la decisión de Fase 3 ($53\times$ de reducción frente al CSV); Delta añade versionado y transacciones ACID sobre ese mismo Parquet. \\
Catálogo de esquema & Unity Catalog & Registra el esquema de plata y oro con control de acceso centralizado, sin definir tablas a mano. \\
Agregación / Oro & Databricks Jobs (mismo notebook) & Calcula Q1, Q2 y Q3 y las escribe como Delta pequeño en \texttt{gold/}. \\
Consumo & Databricks SQL (Serverless) + Power BI & El warehouse arranca en segundos, se apaga solo tras inactividad y cobra por segundo activo; evita un clúster SQL encendido para tan pocas consultas. \\
\bottomrule
\end{tabularx}
\end{table}

La capa de limpieza es donde se resuelven los tres problemas de calidad detectados en la exploración: el casteo de \texttt{end\_station\_id} a entero, el tratamiento de los nulos de destino y el marcado de duraciones anómalas. La capa plata materializa la proyección de cuatro columnas con tipos reducidos que produjo las mejoras de la Fase 3.

\textbf{Pendiente de decidir por el equipo:}

\begin{itemize}
  \item Confirmar si el enunciado pide capas medallón u otra nomenclatura
  \item Decidir si el catálogo de estaciones se une en plata o en oro
  \item Especificar la estrategia de particionado en ADLS Gen2, si aplica (por ejemplo, por año-mes de \texttt{start\_rental\_date\_time})
\end{itemize}
